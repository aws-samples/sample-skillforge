{
  "note": "Servers worth having connected while unused. Eager means they start with the session.",
  "access": "open",
  "loading": "eager",
  "mcpServers": {
    "sqlite-explorer": {
      "command": "uvx",
      "args": ["--with", "mcp<2", "mcp-server-sqlite", "--db-path", "./warehouse.db"]
    }
  }
}
